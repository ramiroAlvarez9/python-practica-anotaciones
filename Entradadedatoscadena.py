#la entrada de datos hace referencia a lo que le pedimos al usuario que nos digite. Para generar una entra de datos, utilizamos la funcion INPUT.
# entonces, creamos la variable especificando que sera de entrada, ejemplo:

#nombre = input("Digita tu nombre: ")

#print(f"Su nombre es {nombre}")

#con la funcion INPUT generamos una variable que almacena un "String".

#Para generar una variable que almacene un dato numerico, sea float o integer, especificaremos eso en el comienzo de la declaracion, ejemplo:

numero = int(input("Digite un numero al cual le aumentar su mismo valor:  "))
         #especificamos variable numerica
         

print(f"El numero resultante es: {numero + numero}")
                                  #sumamos la variable por la misma variable.





